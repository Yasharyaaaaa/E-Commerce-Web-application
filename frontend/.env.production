# Baked into the build by Vite. Points the prod frontend (Vercel) at the
# Render backend for both REST and Socket.io. (Public URL — safe to commit.)
VITE_API_URL=https://shoptalk-backend-4kp9.onrender.com/api
