# Leave unset in dev (Vite proxies /api and /socket.io to localhost:3000).
# In production (e.g. Vercel), set this to your deployed backend API base:
VITE_API_URL=https://shoptalk-backend.onrender.com/api
